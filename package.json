{
  "name": "dog-test",
  "version": "0.1.0",
  "private": true,
  "description": "The DoG Test: a definition-of-good audit for AI roadmap items, run as two LLM agents in conversation.",
  "type": "module",
  "scripts": {
    "audit": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "openai": "^6.39.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
